# Project Rules

Avoid rewriting published git history — force pushing, or rebasing/amending/squashing commits that are already pushed — as it rewrites history and you will likely lose project history.

Commits you push to the connected branch sync back to the editor, so keep the branch in a working state.
